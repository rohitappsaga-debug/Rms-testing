# Environment Variables
# Node Environment: development, production, or test
NODE_ENV=development
# Port Configuration
# The application will automatically try to find a free port starting from the values below.
# If a port is busy, it will increment (e.g., 3000 -> 3001) until it finds a free one.

# Backend Port — the port the Express API and Socket.io will listen on.
PORT=3000

# Frontend Port — the port the Vite dev server will use during development.
FRONTEND_PORT=3002

# Installer Port — the port the web-based installer will use.
INSTALLER_PORT=3005

# Database Configuration
# PostgreSQL connection string
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=SCHEMA
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/restaurant_db?schema=public"

# JWT Configuration
# Secret key for signing tokens - MUST be strong in production (min 32 chars)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
# Access token expiration (e.g., 15m, 1h, 24h)
JWT_EXPIRES_IN=24h
# Refresh token expiration (e.g., 7d, 30d)
JWT_REFRESH_EXPIRES_IN=7d

# CORS Configuration
# Comma-separated list of allowed origins or a single origin
CORS_ORIGIN=http://localhost:3002

# Socket.io Configuration
# Allowed origin for WebSocket connections
SOCKET_CORS_ORIGIN=http://localhost:3002

# Rate Limiting
# Window size in milliseconds (default: 15 minutes)
RATE_LIMIT_WINDOW_MS=900000
# Max requests per window per IP
RATE_LIMIT_MAX_REQUESTS=1000

# Logging
# Log level: error, warn, info, http, debug (default varies by NODE_ENV)
# LOG_LEVEL=info
# Log file path (optional, defaults to logs/combined.log and logs/error.log)
# LOG_FILE=logs/app.log

# File Upload (Optional - if implemented)
# Max file size in bytes (default: 5MB)
MAX_FILE_SIZE=5242880
# Upload directory path
UPLOAD_PATH=uploads/

# External Services (Optional / Future Use)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password
# REDIS_URL=redis://localhost:6379 

